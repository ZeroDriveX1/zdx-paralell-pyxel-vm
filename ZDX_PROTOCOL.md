# ZDX Protocol Layer

Protocol metadata now provides:

- protocol versioning
- message type envelope
- timestamps
- compatibility checks

Future upgrades can negotiate capabilities without breaking existing nodes.
